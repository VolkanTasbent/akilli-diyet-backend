services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: akilli_diyet
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "1"
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
