spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdiyet;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: test-jwt-secret-must-be-at-least-32-chars-long-ok
    expiration-ms: 3600000
